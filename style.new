:root {
    --font-color: 
}

header,footer {
    text-align: center;
}

section {
    display:flex;
    flex-direction: row;
    flex-wrap: wrap;
    margin-left: 10px;
    margin-right: 10px;
    justify-content: center;
    columns: 1;
}